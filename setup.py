from setuptools import setup

setup(name='iris_prediction',
      version='0.1',
      description='Identify iris from sepal and petal information',
      url='http://github.com/mtconley/iris_prediction.git'
      packages=['iris_prediction'],
      zip_safe=False)
